# dotfiles
Personal Linux config files.

## Usage
Repository is structured to be used with [GNU Stow](https://www.gnu.org/software/stow/) and available at `~/dotfiles`:

```
cd ~/dotfiles
stow git
stow nvim
stow starship
```
